Move everything to classes

Find a linter

Find a Lang Server

Figure out how long migrating to typescript would take

Vue-cli

Vim ale, coc, ycm plugins

We should try splitting out the engine to type script as its already VueJS agnostic.

Landmark Placement can be a lot more data driven.
Engine internals should be typed if possible.

We can split the internal and external interface.
